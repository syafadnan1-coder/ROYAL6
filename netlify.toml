# إعدادات Netlify - لا داعي للبناء، الملف جاهز
[build]
  publish = "."
  command = "echo 'Site is pre-built, no build needed'"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "18"
